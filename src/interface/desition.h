#include <bits/stdc++.h>
#include <cstring>

enum CASES {
    HELP = 0,
};
class desition
{
    private:
        void database(void);
        void help(void);
    public:
        void cmd(std::string command);
};
